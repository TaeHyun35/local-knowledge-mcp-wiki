# 진행사항

## 2026-06-15 GitHub 공개 repo 패키지 요구사항 보강

- 루트 `README.md`를 clone 후 30분 안에 raw 자료 1건으로 Wiki draft를 만들고 viewer에서 확인하는 가이드로 재작성했다.
- `AGENTS.md`, `RULES.md`, `skills/wiki-curator/SKILL.md`, `hooks/preflight_check.py`를 추가해 하네스와 Agent 운영지침 요구를 충족했다.
- `tools/` wrapper를 추가해 `tools/mcp_server/server.py`, `tools/mcp_server/smoke_test.py`, `tools/ingest.py`, `tools/validate_wiki.py`, `tools/render_viewer_data.py`로 실행 진입점을 통일했다.
- `viewer/index.html`, `schema/*.json`, `raw/inbox/sample-note.md`, `.github/workflows/harness.yml`, `.gitignore`, `demo/mvp.png`를 추가했다.
- clean GitHub source package `LOCAL_KNOWLEDGE_MCP_WIKI_GITHUB_READY.zip`를 생성했다.

검증:

- `python hooks\preflight_check.py` 통과
- `python tools\mcp_server\smoke_test.py` 통과
- `python tools\validate_wiki.py` 통과
- `python tools\render_viewer_data.py` 통과
- `python tools\ingest.py raw/inbox/sample-note.md --title "Sample Draft"` dry-run 통과
- `maintenance_check.ps1` 통과
- clean source zip 내부 요구 항목 포함, cache 0, old package 0 확인

## 2026-06-15 문서 무결성 및 viewer 연결성 최신화

- Git 추적 대상 문서와 앱 파일 목록을 확인해 루트 README, AGENTS, RULES, Agent SPEC, 제출 README/PRD, Wiki Curator Skill의 실행 안내와 기능 설명을 최신화했다.
- `app/index.html` 기준 실행 URL을 명확히 했고, `viewer/index.html`은 호환용 리다이렉트임을 문서화했다.
- Source Coverage, Evidence Trace, Reading Deck, fixed-anchor Wiki Graph를 README/PRD/SPEC/하네스 규칙에 반영했다.
- 그래프 간선은 노드 좌우 anchor를 기준으로 연결되고, 상단 metadata path는 말줄임 처리되어 UI 겹침이 줄어들도록 구현과 문서를 맞췄다.
- `app/wiki-data.js`를 재생성하고 smoke/quality/preflight 검증을 통과했다.

검증:

- `node --check .\app\app.js` 통과
- `python tools\render_viewer_data.py` 통과
- `python tools\mcp_server\smoke_test.py` 통과
- `python hooks\preflight_check.py` 통과
- `python tools\validate_wiki.py` 통과

파일 갱신 시각: 2026-06-15 19:45:00 +09:00
